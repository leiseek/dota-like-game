{
  "name": "dota-like-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/**/*.test.js",
    "check": "npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.5.4"
  }
}
